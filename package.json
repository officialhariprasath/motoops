{
  "name": "auto-garage-services",
  "private": true,
  "scripts": {
    "dev": "concurrently -n client,server -c blue,green \"npm --prefix client-nextjs run dev\" \"npm --prefix server-nestjs run start:dev\"",
    "dev:client": "npm --prefix client-nextjs run dev",
    "dev:server": "npm --prefix server-nestjs run start:dev"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
